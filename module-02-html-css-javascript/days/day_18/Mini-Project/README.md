transactions.js — stores and exports the transaction data.
report.js — contains reusable functions for calculating totals, creating receipts, and updating transactions.
app.js — imports the modules and prints the final report.
